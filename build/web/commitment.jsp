
<%@taglib prefix="s" uri="/struts-tags"%>
<%@page contentType="text/html" pageEncoding="UTF-8"%>

<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Cam kết bán hàng</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="icon" href="img/favicon (1).ico" type="image/x-icon" />	
        <link href="eCommerceAssets/styles/header.css" rel="stylesheet" type="text/css">	
        <link href="eCommerceAssets/styles/layout.css" rel="stylesheet" type="text/css">
        <link href="eCommerceAssets/styles/footer.css" rel="stylesheet" type="text/css">
        <link href="eCommerceAssets/styles/commitment.css" rel="stylesheet" type="text/css">	
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
        <!--<script src="js/thien.js" type="text/javascript"></script>-->

    </head>

    <body class="bg">
        <div id="mainWrapper">
            <button onclick="topFunction()" id="button-to-top" title="Click to top"><i class="fa fa-chevron-up"></i>
            </button>

            <%@include file="WEB-INF/top_nav.jsp" %>

            <div id="commitment" class="animate-bottom-offer">
                <div id="wrap-commitment">
                    <h1 class="center" style="color: coral">CAM KẾT BÁN HÀNG</h1>
                    <p class="center"><a href="index.jsp">Trang chủ</a> /  Cam kết bán hàng</p>
                    <hr>
                    <h4>Là một trong những doanh nghiệp hoạt động có uy tín lâu năm trên lĩnh vực này, đại lý chính thức của các thương hiệu lớn trên thế giới như : Bandai , HotToys,...
                        LuonaShop xin cam kết chính sách bán hàng và chính sách chất lượng với sản phẩm được cung cấp như sau:</h4>
                    <p>- Laptop4pro cam kết tất cả hàng hoá đều là sản phẩm chính hãng, nguồn gốc rõ ràng minh bạch, chính hãng từ các nhà sản xuất.</p>
                    <p>- Nếu quý khách nghi ngờ sản phẩm đã mua là hàng giả,hàng kém chất lượng , đừng ngần ngại phản hồi ngay cho để nhận được tiền hoàn lại 100% .</p>
                    <p>- Hàng chính hãng được phân phối chính thức tại Việt Nam sẽ có tem/ phiếu bảo hành của nhà phân phối, có đầy đủ CO, CQ, giá đã bao gồm VAT, bảo hành tại LuonaShop, tại hãng hoặc trung tâm bảo hành ủy quyền của hãng, chính sách bảo hành theo quy định của hãng  .</p>
                    <p>- Sản phẩm được bày bán giá cả cạnh tranh, có giá thấp hơn hoặc bằng giá thị trường theo quy định của nhà sản xuất. LuonaShop cam kết bán hàng đúng giá – đúng chất lượng.</p>
                    <p>· Với mục tiêu mang đến cho khách hàng sự an tâm tuyệt đối khi mua hàng tại LuonaShop, chúng tôi áp dụng chính sách đổi/trả hàng cho quý khách hàng mua hàng như sau<i>( khách hàng có thể sẽ chịu một khoản chi phí cho việc vận chuyển khi đổi/trả hàng )</i></p>
                    <p>Điều Kiện Áp Dụng:</p>
                    <p>- Trong vòng 15 ngày kể từ khi khách hàng mua sản phẩm (Tính theo hóa đơn mua hàng)</p>
                    <p>- Sản phẩm đổi/trả phải còn nguyên trạng, vỏ bên ngoài và thân máy không bị trầy xước hoặc có dấu hiệu của va đập, ẩm ướt, hư hỏng do lỗi người sử dụng.</p>
                    <p>- Phiếu bảo hành (nếu có) và tem sản phẩm của hãng (trong & ngoài) phải còn nguyên vẹn.</p>
                    <p>- Hàng đổi/ trả lại phải đổi/ trả lại đầy đủ hàng khuyến mãi kèm theo khi mua sản phẩm.</p>
                    <p>- Hoàn tiền 100% giá trị hàng hoá bán ra nếu hàng hoá bán ra không đúng cam kết, sai nguồn gốc .</p>
                    <p>-Hàng hóa bị lỗi còn mới, xác định là lỗi kỹ thuật do nhà sản xuất (có xác nhận của hãng), linh kiện, phụ kiện kèm theo còn mới, hộp/ thùng còn nguyên vẹn, đầy đủ chứng từ như khi giao hàng, riêng màn hình phải từ 3 điểm chết trở lên mới được đổi mới.</p>
                    <h4>Có hai phương thức mua hàng dành cho khách hàng ở ngoài TP.HCM :</h4>
                    <h4>1.Gửi hàng thanh toán trước miễn phí hoàn toàn phí giao hàng:</h4>
                    <p>- Bạn đặt hàng bằng cách gọi điện thoại hoặc gữi email vào địa chỉ mail shop mình benicacronaldo@gmail.com sau đó bạn chuyển khoản 100% vào tài khoản shop , mình chuyển hàng tận nhà cho bạn . Bạn kiểm tra hàng hóa nếu có vấn đề bạn gọi điện cho shop để được trả hoặc đổi con khác . Ngoài ra nếu hàng hóa có vấn đền về lỗi phần cứng bạn vẫn được đổi trả trong vòng 15 ngày .</p>
                    <h4>2. Gửi hàng thanh toán bằng dich vụ COD</h4>
                    <p>-Bạn đặt hàng bằng cách gọi điện thoại hoặc gữi email vào địa chỉ mail cty mình laptop4pro@gmail.com . Bên mình sẽ gữi hàng ra cho bạn , bạn nhận hàng và nếu không có vấn đề gì thanh toán cho vận chuyển và bạn phải trả thêm phí thu hộ (phí COD) là 2% trên tổng tiền thanh toán tùy địa phương .Phí giao nhận bạn vẫn miễn phí chỉ trả phí COD .</p>
                    <p>Ngoài ra nếu hàng hóa có vấn đền về lỗi phần cứng bạn vẫn được đổi trả hoặc hoàn tiền trong vòng 15 ngày .</p>
                    <h3 class="center">Hi vọng bài viết sẽ có ích cho các bạn</h3>

                </div>	









            </div>


            <%@include file="WEB-INF/footer.jsp" %>











        </div>





        <div class="aa">
            <div class="bodya">
                <canvas id="sakura"></canvas>
                <div class="btnbg">
                </div>
                <!-- sakura shader -->
                <script id="sakura_point_vsh" type="x-shader/x_vertex">
                    uniform mat4 uProjection;
                    uniform mat4 uModelview;
                    uniform vec3 uResolution;
                    uniform vec3 uOffset;
                    uniform vec3 uDOF;  //x:focus distance, y:focus radius, z:max radius
                    uniform vec3 uFade; //x:start distance, y:half distance, z:near fade start

                    attribute vec3 aPosition;
                    attribute vec3 aEuler;
                    attribute vec2 aMisc; //x:size, y:fade

                    varying vec3 pposition;
                    varying float psize;
                    varying float palpha;
                    varying float pdist;

                    //varying mat3 rotMat;
                    varying vec3 normX;
                    varying vec3 normY;
                    varying vec3 normZ;
                    varying vec3 normal;

                    varying float diffuse;
                    varying float specular;
                    varying float rstop;
                    varying float distancefade;

                    void main(void) {
                    // Projection is based on vertical angle
                    vec4 pos = uModelview * vec4(aPosition + uOffset, 1.0);
                    gl_Position = uProjection * pos;
                    gl_PointSize = aMisc.x * uProjection[1][1] / -pos.z * uResolution.y * 0.5;

                    pposition = pos.xyz;
                    psize = aMisc.x;
                    pdist = length(pos.xyz);
                    palpha = smoothstep(0.0, 1.0, (pdist - 0.1) / uFade.z);

                    vec3 elrsn = sin(aEuler);
                    vec3 elrcs = cos(aEuler);
                    mat3 rotx = mat3(
                    1.0, 0.0, 0.0,
                    0.0, elrcs.x, elrsn.x,
                    0.0, -elrsn.x, elrcs.x
                    );
                    mat3 roty = mat3(
                    elrcs.y, 0.0, -elrsn.y,
                    0.0, 1.0, 0.0,
                    elrsn.y, 0.0, elrcs.y
                    );
                    mat3 rotz = mat3(
                    elrcs.z, elrsn.z, 0.0, 
                    -elrsn.z, elrcs.z, 0.0,
                    0.0, 0.0, 1.0
                    );
                    mat3 rotmat = rotx * roty * rotz;
                    normal = rotmat[2];

                    mat3 trrotm = mat3(
                    rotmat[0][0], rotmat[1][0], rotmat[2][0],
                    rotmat[0][1], rotmat[1][1], rotmat[2][1],
                    rotmat[0][2], rotmat[1][2], rotmat[2][2]
                    );
                    normX = trrotm[0];
                    normY = trrotm[1];
                    normZ = trrotm[2];

                    const vec3 lit = vec3(0.6917144638660746, 0.6917144638660746, -0.20751433915982237);

                    float tmpdfs = dot(lit, normal);
                    if(tmpdfs < 0.0) {
                    normal = -normal;
                    tmpdfs = dot(lit, normal);
                    }
                    diffuse = 0.4 + tmpdfs;

                    vec3 eyev = normalize(-pos.xyz);
                    if(dot(eyev, normal) > 0.0) {
                    vec3 hv = normalize(eyev + lit);
                    specular = pow(max(dot(hv, normal), 0.0), 20.0);
                    }
                    else {
                    specular = 0.0;
                    }

                    rstop = clamp((abs(pdist - uDOF.x) - uDOF.y) / uDOF.z, 0.0, 1.0);
                    rstop = pow(rstop, 0.5);
                    //-0.69315 = ln(0.5)
                    distancefade = min(1.0, exp((uFade.x - pdist) * 0.69315 / uFade.y));
                    }
                </script>
                <script id="sakura_point_fsh" type="x-shader/x_fragment">
                    #ifdef GL_ES
                    //precision mediump float;
                    precision highp float;
                    #endif

                    uniform vec3 uDOF;  //x:focus distance, y:focus radius, z:max radius
                    uniform vec3 uFade; //x:start distance, y:half distance, z:near fade start

                    const vec3 fadeCol = vec3(0.08, 0.03, 0.06);

                    varying vec3 pposition;
                    varying float psize;
                    varying float palpha;
                    varying float pdist;

                    //varying mat3 rotMat;
                    varying vec3 normX;
                    varying vec3 normY;
                    varying vec3 normZ;
                    varying vec3 normal;

                    varying float diffuse;
                    varying float specular;
                    varying float rstop;
                    varying float distancefade;

                    float ellipse(vec2 p, vec2 o, vec2 r) {
                    vec2 lp = (p - o) / r;
                    return length(lp) - 1.0;
                    }

                    void main(void) {
                    vec3 p = vec3(gl_PointCoord - vec2(0.5, 0.5), 0.0) * 2.0;
                    vec3 d = vec3(0.0, 0.0, -1.0);
                    float nd = normZ.z; //dot(-normZ, d);
                    if(abs(nd) < 0.0001) discard;

                    float np = dot(normZ, p);
                    vec3 tp = p + d * np / nd;
                    vec2 coord = vec2(dot(normX, tp), dot(normY, tp));

                    //angle = 15 degree
                    const float flwrsn = 0.258819045102521;
                    const float flwrcs = 0.965925826289068;
                    mat2 flwrm = mat2(flwrcs, -flwrsn, flwrsn, flwrcs);
                    vec2 flwrp = vec2(abs(coord.x), coord.y) * flwrm;

                    float r;
                    if(flwrp.x < 0.0) {
                    r = ellipse(flwrp, vec2(0.065, 0.024) * 0.5, vec2(0.36, 0.96) * 0.5);
                    }
                    else {


                    r = ellipse(flwrp, vec2(0.065, 0.024) * 0.5, vec2(0.58, 0.96) * 0.5);
                    }

                    if(r > rstop) discard;

                    vec3 col = mix(vec3(1.0, 0.8, 0.75), vec3(1.0, 0.9, 0.87), r);
                    float grady = mix(0.0, 1.0, pow(coord.y * 0.5 + 0.5, 0.35));
                    col *= vec3(1.0, grady, grady);
                    col *= mix(0.8, 1.0, pow(abs(coord.x), 0.3));
                    col = col * diffuse + specular;

                    col = mix(fadeCol, col, distancefade);

                    float alpha = (rstop > 0.001)? (0.5 - r / (rstop * 2.0)) : 1.0;
                    alpha = smoothstep(0.0, 1.0, alpha) * palpha;

                    gl_FragColor = vec4(col * 0.5, alpha);
                    }
                </script>
                <!-- effects -->
                <script id="fx_common_vsh" type="x-shader/x_vertex">
                    uniform vec3 uResolution;
                    attribute vec2 aPosition;

                    varying vec2 texCoord;
                    varying vec2 screenCoord;

                    void main(void) {
                    gl_Position = vec4(aPosition, 0.0, 1.0);
                    texCoord = aPosition.xy * 0.5 + vec2(0.5, 0.5);
                    screenCoord = aPosition.xy * vec2(uResolution.z, 1.0);
                    }
                </script>
                <script id="bg_fsh" type="x-shader/x_fragment">
                    #ifdef GL_ES
                    //precision mediump float;
                    precision highp float;
                    #endif

                    uniform vec2 uTimes;

                    varying vec2 texCoord;
                    varying vec2 screenCoord;

                    void main(void) {
                    vec3 col;
                    float c;
                    vec2 tmpv = texCoord * vec2(3, 0.9) - vec2(0.95, 1);
                    c = exp(-pow(length(tmpv) * 1.5, 0.3));
                    col = mix(vec3(0, 0, 0), vec3(1,1,1) * 1.5, c);
                    gl_FragColor = vec4(col * 0.5, 1.0);

                    }
                </script>
                <script id="fx_brightbuf_fsh" type="x-shader/x_fragment">
                    #ifdef GL_ES
                    //precision mediump float;
                    precision highp float;
                    #endif
                    uniform sampler2D uSrc;
                    uniform vec2 uDelta;

                    varying vec2 texCoord;
                    varying vec2 screenCoord;

                    void main(void) {
                    vec4 col = texture2D(uSrc, texCoord);
                    gl_FragColor = vec4(col.rgb * 2.0 - vec3(0.5), 1.0);
                    }
                </script>
                <script id="fx_dirblur_r4_fsh" type="x-shader/x_fragment">
                    #ifdef GL_ES
                    //precision mediump float;
                    precision highp float;
                    #endif
                    uniform sampler2D uSrc;
                    uniform vec2 uDelta;
                    uniform vec4 uBlurDir; //dir(x, y), stride(z, w)

                    varying vec2 texCoord;
                    varying vec2 screenCoord;

                    void main(void) {
                    vec4 col = texture2D(uSrc, texCoord);
                    col = col + texture2D(uSrc, texCoord + uBlurDir.xy * uDelta);
                    col = col + texture2D(uSrc, texCoord - uBlurDir.xy * uDelta);
                    col = col + texture2D(uSrc, texCoord + (uBlurDir.xy + uBlurDir.zw) * uDelta);
                    col = col + texture2D(uSrc, texCoord - (uBlurDir.xy + uBlurDir.zw) * uDelta);
                    gl_FragColor = col / 5.0;
                    }
                </script>
                <!-- effect fragment shader template -->
                <script id="fx_common_fsh" type="x-shader/x_fragment">
                    #ifdef GL_ES
                    //precision mediump float;
                    precision highp float;
                    #endif
                    uniform sampler2D uSrc;
                    uniform vec2 uDelta;

                    varying vec2 texCoord;
                    varying vec2 screenCoord;

                    void main(void) {
                    gl_FragColor = texture2D(uSrc, texCoord);
                    }
                </script>
                <!-- post processing -->
                <script id="pp_final_vsh" type="x-shader/x_vertex">
                    uniform vec3 uResolution;
                    attribute vec2 aPosition;
                    varying vec2 texCoord;
                    varying vec2 screenCoord;
                    void main(void) {
                    gl_Position = vec4(aPosition, 0.0, 1.0);
                    texCoord = aPosition.xy * 0.5 + vec2(0.5, 0.5);
                    screenCoord = aPosition.xy * vec2(uResolution.z, 1.0);
                    }
                </script>
                <script id="pp_final_fsh" type="x-shader/x_fragment">
                    #ifdef GL_ES
                    //precision mediump float;
                    precision highp float;
                    #endif
                    uniform sampler2D uSrc;
                    uniform sampler2D uBloom;
                    uniform vec2 uDelta;
                    varying vec2 texCoord;
                    varying vec2 screenCoord;
                    void main(void) {
                    vec4 srccol = texture2D(uSrc, texCoord) * 2.0;
                    vec4 bloomcol = texture2D(uBloom, texCoord);
                    vec4 col;
                    col = srccol + bloomcol * (vec4(1.0) + srccol);
                    col *= smoothstep(1.0, 0.0, pow(length((texCoord - vec2(0.5)) * 2.0), 1.2) * 0.5);
                    col = pow(col, vec4(0.45454545454545)); //(1.0 / 2.2)

                    gl_FragColor = vec4(col.rgb, 1.0);
                    gl_FragColor.a = 1.0;
                    }
                </script>
            </div>
            <script src="js/effect.js"></script>	
        </div>		





        <script src="js/scrolls-thien.js" type="text/javascript"></script>		







































    </body>
</html>
